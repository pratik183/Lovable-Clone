spring:
  application:
    name: lovable-clone
    description: This is my Lovable Clone application

  datasource:
    url: jdbc:postgresql://localhost:9000/testDB
    username: testUser
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format-sql: true
# add jwt secret key

#add stripe api, webhook secret

client.url: http://localhost:8080